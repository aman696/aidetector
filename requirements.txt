numpy
scipy
opencv-python
pillow
scikit-learn
matplotlib
pytest
fastapi
uvicorn
python-multipart  # required by FastAPI for image uploads (app.py /api/detect)
pydantic
prometheus-client  # /metrics endpoint for the monitoring stack (see MONITORING.md)

# Optional (hybrid v2 embeddings). The repo runs classical-only without these;
# install for the DINOv2 path: pip install torch timm
torch
timm

# Optional (GPU-accelerated SVM training via RAPIDS cuML). CPU sklearn is the
# default/fallback; install to use `python -m src.train_unified --gpu`:
# pip install --extra-index-url=https://pypi.nvidia.com cuml-cu13
# cuml-cu13

# Optional: external-dataset fetch for AR research (scripts/fetch_openfake.py). Not needed at runtime.
datasets>=3.0
